{% extends 'base.html.twig' %}

{% block title %}User Management{% endblock %}

{% block stylesheets %}
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        .header {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        .header h1 {
            margin: 0;
            color: #333;
            font-size: 2.5em;
        }
        .section {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        .section h2 {
            color: #333;
            margin-bottom: 20px;
            border-bottom: 3px solid #667eea;
            padding-bottom: 10px;
        }
        .user-table {
            width: 100%;
            border-collapse: collapse;
        }
        .user-table th,
        .user-table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
        }
        .user-table th {
            background: #f8f9fa;
            font-weight: 600;
            color: #333;
        }
        .user-table tr:hover {
            background: #f8f9fa;
        }
        .badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.85em;
            font-weight: bold;
        }
        .badge-admin {
            background: #dc3545;
            color: white;
        }
        .badge-professor {
            background: #4facfe;
            color: white;
        }
        .badge-student {
            background: #28a745;
            color: white;
        }
        .btn-back {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
        .btn-back:hover {
            background: #764ba2;
            color: white;
        }
        .btn-delete {
            background: #dc3545;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }
        .btn-delete:hover {
            background: #c82333;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container">
    <div class="header">
        <h1>👥 User Management</h1>
        <a href="{{ path('app_admin_dashboard') }}" class="btn-back">← Back to Admin Dashboard</a>
    </div>

    <div class="section">
        <h2>👨‍💼 Administrators</h2>
        {% if admins|length > 0 %}
            <table class="user-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for admin in admins %}
                        <tr>
                            <td>{{ admin.name ?? 'N/A' }}</td>
                            <td>{{ admin.email }}</td>
                            <td><span class="badge badge-admin">Admin</span></td>
                            <td>
                                <a href="{{ path('app_user_show', {'id': admin.id}) }}" class="btn-delete" style="background: #667eea;">View</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>No administrators found.</p>
        {% endif %}
    </div>

    <div class="section">
        <h2>👨‍🏫 Professors</h2>
        {% if professors|length > 0 %}
            <table class="user-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for professor in professors %}
                        <tr>
                            <td>{{ professor.name ?? 'N/A' }}</td>
                            <td>{{ professor.email }}</td>
                            <td><span class="badge badge-professor">Professor</span></td>
                            <td>
                                <a href="{{ path('app_user_show', {'id': professor.id}) }}" class="btn-delete" style="background: #667eea;">View</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>No professors found.</p>
        {% endif %}
    </div>

    <div class="section">
        <h2>🎓 Students</h2>
        {% if students|length > 0 %}
            <table class="user-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for student in students %}
                        <tr>
                            <td>{{ student.name ?? 'N/A' }}</td>
                            <td>{{ student.email }}</td>
                            <td><span class="badge badge-student">Student</span></td>
                            <td>
                                <a href="{{ path('app_user_show', {'id': student.id}) }}" class="btn-delete" style="background: #667eea; margin-right: 8px;">View</a>
                                <a href="{{ path('app_assign_students', {'student_id': student.id}) }}" class="btn-delete" style="background: #28a745;">Assign to Class</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>No students found.</p>
        {% endif %}
    </div>
</div>
{% endblock %}

